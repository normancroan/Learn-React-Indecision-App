{
  "name": "indecision",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.0",
    "babel-preset-react": "^6.24.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src -d public",
    "build-with-reload": "babel src -d public --watch",
    "build-playground": "babel playground/counter.js --out-file=public/scripts/app.js --watch"
  },
  "author": "Norman Croan",
  "license": "ISC"
}
